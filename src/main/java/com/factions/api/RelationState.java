package com.factions.api;

/**
 * Enum representing the diplomatic state between two factions.
 * Used for quick lookups in the Faction interface.
 */
public enum RelationState {
    NEUTRAL,
    ALLY,
    ENEMY,
    TRUCE
}
